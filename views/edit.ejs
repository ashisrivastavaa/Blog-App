<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Profile</title>
  <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>
<body>
  <div class="w-full min-h-screen bg-zinc-900 text-white flex flex-col items-center py-10 gap-6">
    
 
    <a class="bg-red-500 rounded-md px-3 py-2 self-end mr-10" href="/logout">Logout</a>
    
    <h5 class="text-lg mb-5">You can Edit Post here </h5>

   
    <form action="/update/<%= post._id%>" method="POST" class="w-full max-w-xl flex flex-col gap-4">
      <textarea name="content" rows="4" placeholder="What's in your mind?"
        class="p-3 outline-none w-full resize-none bg-transparent border-2 border-zinc-800 rounded-md "><%=post.content%></textarea>
      <input type="submit" value="Update Post"
        class="px-3 w-40 text-sm rounded-md py-2 bg-yellow-500 cursor-pointer hover:bg-yellow-600 text-black">
    </form>


  </div>
</body>
</html>
